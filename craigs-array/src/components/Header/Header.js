import React, { Component } from 'react';

class Header extends Component {
  render() {
    return (
      <header class="header navbar-fixed-top">Testing</header>
    
    );
  }
}

export default Header;
